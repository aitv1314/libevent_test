#include <iostream>
#include <event.h>
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** agrv)
{
    /* 创建socket */

    /* connect到server */

    /* 创建event base */
    /* 添加事件到base，指定read_cb */

    /* 在base上，创建stdin event */、

    /* 添加事件 */

    /* base循环调度 */

    return 0;
}

